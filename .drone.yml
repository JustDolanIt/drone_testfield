pipeline:
  git_diff:
    image: alpine/git
    commands:
      - git show

  js_lint:
    image: alpine
    commands:
      - apk add jq
      - jq '.' test.json

  html_validator:
    image: validator/validator
    commands:
      - pwd
      - ls *.html | xargs -I {} java -jar /vnu.jar {}
